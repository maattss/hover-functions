query GetUserAndAchievements($user_id: String!) {
  unachievedachievements(args: { uid: $user_id }) {
    id
    name
    description
    created_at
    achievement_type
    rule
  }
  user(id: $user_id ) {
    id
    totalScore
  }
}